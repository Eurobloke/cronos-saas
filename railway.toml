[build]
builder = "nixpacks"

[deploy]
startCommand = "python init_db.py && gunicorn run:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --preload"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "cronos-saas"
